/**
 * Landing page URL helper.
 *
 * Now that we've consolidated to lawlens.io (no subdomain),
 * all paths are relative — no cross-domain routing needed.
 */
export function getAppUrl(path: string): string {
  return path;
}
